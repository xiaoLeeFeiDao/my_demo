{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "popos_Launch",
        "program": "${workspaceFolder}/bin/test",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}